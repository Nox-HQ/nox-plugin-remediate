# Fixture layout

Each deterministic fixer should use this structure:

`<fixer-id>/<language>/{before,after,tests}`

Example:

`web-sec-001/go/before/server.go`

`web-sec-001/go/after/server.go`

`web-sec-001/go/tests/assertions.json`
